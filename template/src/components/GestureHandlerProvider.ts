import { GestureHandlerRootView } from 'react-native-gesture-handler';
import styled from 'styled-components/native';

export const GestureHandlerProvider = styled(GestureHandlerRootView)`
  flex: 1;
`;
